pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                echo 'Package'
                sh './mvnw package -DskipTests'
            }
        }
        stage('Move') {
            steps {
              sh 'mv target/*.jar /home/var/data/'
            }
        }
        stage('Deploy') {
            steps {
              sh 'systemctl restart petclinic'
            }
        }
    }
}
